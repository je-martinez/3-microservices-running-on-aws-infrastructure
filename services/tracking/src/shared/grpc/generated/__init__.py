"""Generated protobuf/gRPC modules — DO NOT EDIT BY HAND.

Produced from `proto/users.proto` — the surface this service CALLS, and the only
one it generates stubs for since JE-108 removed the served `tracking.v1` surface —
by `scripts/generate_grpc_stubs.py`, and committed deliberately (that script's
docstring explains why, and `tests/test_grpc_stubs.py` fails if these files drift
from the proto).

Only the CLIENT half of the generated stub is used. `UsersServicer` and
`add_UsersServicer_to_server` are also emitted by protoc; nothing in `src/`
subclasses or calls them.

Ruff excludes this directory: it is machine output, not hand-maintained style.
"""
