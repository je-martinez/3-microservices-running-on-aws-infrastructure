{
  "_comment_Logging": [
    "Kept in sync with appsettings.json. This file is NOT loaded by the",
    "containerized service (ASPNETCORE_ENVIRONMENT is unset in the Dockerfile and",
    "compose, so the host runs as Production) — it only applies to a local",
    "`dotnet run`. See Program.cs for the overrides that actually take effect."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "System.Net.Http.HttpClient.OtlpTraceExporter": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  }
}
