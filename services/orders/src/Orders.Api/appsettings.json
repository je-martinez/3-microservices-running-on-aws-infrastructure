{
  "_comment_Logging": [
    "Serilog is configured in code (Program.cs, UseSerilog) and does NOT call",
    "ReadFrom.Configuration, so these levels do not drive the JSON logs that reach",
    "the collector — the MinimumLevel.Override calls in Program.cs do. These two",
    "noise categories are mirrored here so anything reading the standard",
    "Microsoft.Extensions.Logging pipeline (e.g. `dotnet run` outside the",
    "container, or a future ReadFrom.Configuration) agrees with Program.cs.",
    "Warning, never None: real export/SQL failures must still surface."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "System.Net.Http.HttpClient.OtlpTraceExporter": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*"
}
