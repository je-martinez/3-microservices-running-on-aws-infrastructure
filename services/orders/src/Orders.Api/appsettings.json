{
  "_comment_Logging": [
    "Serilog is configured in code (Program.cs, UseSerilog) and does NOT call",
    "ReadFrom.Configuration, so these levels do not drive the JSON logs that reach",
    "the collector — the MinimumLevel.Override in Program.cs does. Mirrored here so",
    "anything reading the standard Microsoft.Extensions.Logging pipeline (e.g.",
    "`dotnet run` outside the container) agrees with Program.cs.",
    "Warning, never None: real export failures must still surface.",
    "EF Core's Database.Command is intentionally NOT listed: its SQL is ROUTED to",
    "the dedicated `sql` stream by the collector, not suppressed — see",
    "observability/otel-collector-config.yaml (filter/only_sql)."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "System.Net.Http.HttpClient.OtlpTraceExporter": "Warning"
    }
  },
  "AllowedHosts": "*"
}
