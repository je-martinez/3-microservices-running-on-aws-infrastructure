{
  "name": "@3mrai/users",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "prisma": "prisma"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@prisma/client": "^6.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "vitest": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
