{
  "//": "GENERATED CONTRACT — the real proxy.conf.json is written by `make env-file` and gitignored, because Floci mints a new <api-id> on every terraform apply. This file documents the shape only; nothing reads it.",
  "//2": "Why a proxy at all: nothing in this repo sends CORS headers, so ng serve must be same-origin. The browser calls /v1/* on localhost:4200 and this forwards it to the gateway.",
  "//3": "target is localhost:4566 — the HOST port mapping, which is what `ng serve` (running outside Docker) can reach. The container-side equivalent lives in apps/web/nginx.conf and addresses Floci as `floci:4566` instead.",
  "//4": "The $default stage segment is a LITERAL. http-proxy-middleware performs no shell or nginx expansion, so it is written plainly here — unlike nginx.conf, which must percent-encode it as %24.",
  "/v1": {
    "target": "http://localhost:4566",
    "secure": false,
    "changeOrigin": false,
    "pathRewrite": {
      "^/v1": "/restapis/<api-id>/$default/_user_request_/v1"
    }
  }
}
