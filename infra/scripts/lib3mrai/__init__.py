"""Shared helpers for the 3MRAI infrastructure scripts.

The scripts themselves stay colocated with the Terraform modules that invoke
them; only the logic they share lives here. See [[scripting-language]]
"""
