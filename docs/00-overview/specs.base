filters:
  and:
    - 'type == "spec"'
views:
  - type: table
    name: "All Specs"
    groupBy:
      property: note.area
      direction: ASC
    order:
      - note.title
      - note.area
      - note.status
