filters:
  and:
    - 'type == "runbook"'
views:
  - type: table
    name: "All Runbooks"
    groupBy:
      property: note.area
      direction: ASC
    order:
      - note.title
      - note.area
      - 'note.integration-status'
      - 'note.verified-on'
