filters:
  and:
    - type == "runbook"
views:
  - type: table
    name: All Runbooks
    groupBy:
      property: area
      direction: ASC
    order:
      - title
      - area
      - integration-status
      - verified-on
