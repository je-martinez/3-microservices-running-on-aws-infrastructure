filters:
  and:
    - 'type == "adr"'
views:
  - type: table
    name: "All ADRs"
    groupBy:
      property: note.status
      direction: ASC
    order:
      - note.title
      - note.status
      - note.id
